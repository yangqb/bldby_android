<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bldby.shoplibrary">

    <application>
        <activity android:name="com.hyphenate.easeui.customer.ImActivity"></activity>
        <activity android:name="com.hyphenate.easeui.customer.CustomerserviceActivity" />
        <activity android:name=".settlement.SettlementMainActivity" />
        <activity android:name=".shopping.GoodsShoppingActivity" />
        <activity android:name=".goods.GoodsSeachActivity" />
        <activity android:name=".seach.SeachActivity" />
        <activity android:name=".goods.EvaluateActivity" />
        <activity android:name=".goods.GoosDetailActivity" />
        <activity android:name=".ShopMainActivity" />
        <activity android:name=".seach.SeachDetailActivity" />
        <meta-data
            android:name="EASEMOB_APPKEY"
            android:value="1108200528098080#bianlidabenying" /> <!-- 声明SDK所需的service SDK核心功能 -->
        <service
            android:name="com.hyphenate.chat.EMChatService"
            android:exported="true" />
        <service
            android:name="com.hyphenate.chat.EMJobService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" /> <!-- 声明SDK所需的receiver -->
        <receiver android:name="com.hyphenate.chat.EMMonitorReceiver">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REMOVED" />

                <data android:scheme="package" />
            </intent-filter>
            <!-- 可选filter -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>